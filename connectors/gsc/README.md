# Google Search Console connector

Production integration point for Search Console query/page performance. Keep credentials outside the repository. The connector should expose normalized rows containing query, page, clicks, impressions, CTR and position.
